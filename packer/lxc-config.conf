# WazuhBOTS — LXC config for Packer build phase
# Minimal config; the full Docker-in-LXC settings are applied
# by the post-processor when importing into Proxmox.

lxc.include = /usr/share/lxc/config/common.conf
lxc.include = /usr/share/lxc/config/userns.conf
lxc.arch    = linux64
